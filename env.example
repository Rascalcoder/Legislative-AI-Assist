# =============================================================================
# Legislative AI Assist - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual API keys
# NEVER commit .env to version control (it's in .gitignore)
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider API Keys (Required)
# -----------------------------------------------------------------------------
# Get your OpenAI API key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-...

# Get your Anthropic API key: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Get your Google AI API key: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=AIza...

# -----------------------------------------------------------------------------
# Supabase Configuration (Required)
# -----------------------------------------------------------------------------
# Get your Supabase URL and keys: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# -----------------------------------------------------------------------------
# Application Configuration (Optional)
# -----------------------------------------------------------------------------
# Server port (default: 8000 for local, Cloud Run uses $PORT env var)
# PORT=8000

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Enable debug mode (set to "true" for development)
# DEBUG=false

# -----------------------------------------------------------------------------
# Frontend Configuration (Optional)
# -----------------------------------------------------------------------------
# API base URL for frontend (automatically set by deployment platforms)
# API_BASE_URL=http://localhost:8000/api/v1

# -----------------------------------------------------------------------------
# Database Configuration (Optional - if not using Supabase defaults)
# -----------------------------------------------------------------------------
# PostgreSQL connection string (if self-hosting)
# DATABASE_URL=postgresql://user:password@localhost:5432/legislative_ai

# -----------------------------------------------------------------------------
# Security Configuration (Production)
# -----------------------------------------------------------------------------
# Allowed origins for CORS (comma-separated list)
# Replace with your actual frontend domain(s) in production
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Secret key for session management (generate with: openssl rand -hex 32)
# SECRET_KEY=your-secret-key-here

# -----------------------------------------------------------------------------
# Rate Limiting (Optional)
# -----------------------------------------------------------------------------
# Maximum requests per minute per IP
# RATE_LIMIT_PER_MINUTE=60

# -----------------------------------------------------------------------------
# Monitoring & Analytics (Optional)
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking
# SENTRY_DSN=https://...@sentry.io/...

# Google Analytics tracking ID
# GA_TRACKING_ID=G-XXXXXXXXXX

# -----------------------------------------------------------------------------
# Notes:
# -----------------------------------------------------------------------------
# 1. All API keys shown above are examples - replace with your actual keys
# 2. For production deployment:
#    - Use environment variables in your hosting platform (Vercel, Cloud Run, etc.)
#    - Never commit .env to Git
#    - Rotate keys regularly
# 3. See README.md for detailed setup instructions

